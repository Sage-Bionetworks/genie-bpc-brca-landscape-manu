---
title: "Surv step 2"
author: "Alex Paynter"
output: html_document
editor_options: 
  chunk_output_type: console
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = F, include = F,
                      message = F, warning = F)
```

```{r}
library(cli) # prevents an error on version reliance.
library(readr)
library(vctrs)
library(rlang)
library(here)
library(dplyr)
library(tidyr)
library(stringr)
library(magrittr)
library(janitor)
library(glue)
library(ggplot2)
library(huxtable)

library(survival)
library(survminer)

purrr::walk(
  .x = here("R", dir(here("R"))), 
  .f = source
)
```

```{r,load_data}
# Can later be combined with the other survival analysis script.
surv_dat <- load(here("data", "surv_dat.rds"))

```

